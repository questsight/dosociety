.navigator__formLogin
  background themeColor
  text-align center
  border-radius: 30px
  margin-right 10px
  a
    color lightColor
    text-decoration none
    &:hover, &:focus, &:active
      color accentColor
      text-decoration none